# sfx

现成音效（Mixkit）。**mp3 因许可不入库**（Mixkit Free License 允许用于成品，但不允许再分发素材文件本身）。

- 台账：`SOURCES-mixkit.json`（id / 名称 / 直链 / 来源页）
- 抓取：`python ../tools/fetch_sfx.py`（assets.mixkit.co 直链）
- 硬规：音效只用现成、禁一切生成通道；转场类常用段 0.4–0.8s
